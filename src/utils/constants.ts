// export const OrderAmtAboveWhichFreeDeliveryInRs = 500;
// We now have a flat delivery charge of Rs 49 per order.
export const DeliveryChargeInRs = 49;

const FifteenMinsInMs = 15 * 60 * 1000;
export const maxOrderPendingTimeInMs = FifteenMinsInMs;